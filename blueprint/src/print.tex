% print.tex — printable (pdf) version of the weber_general_n blueprint, r20. Build from this directory:
%   pdflatex -interaction=nonstopmode print.tex  (twice)
% The proofs are the single-source files ../../proofs/*.tex shared with the paper.
\documentclass[11pt]{report}
\usepackage[a4paper,margin=2.5cm]{geometry}
\usepackage{expl3}
\usepackage{amsmath,amssymb,amsthm,mathtools,booktabs}
\usepackage[unicode,colorlinks=true,linkcolor=blue,urlcolor=magenta,citecolor=blue,pdftitle={Componentwise saturation-height bounds for relative class-number growth in the cyclotomic Z2- and Z3-towers -- Blueprint},pdfauthor={Hiroki Fukui},pdfsubject={Blueprint: statements, dependency graph and full proofs; version in CITATION.cff; commit SHA and immutable tag in the GitHub Release metadata},pdfkeywords={Weber class number problem, cyclotomic Z2-extension, blueprint, Lean 4, certificates}]{hyperref}
\tolerance=2000 % blueprint typesetting: prefer a loose line to an overfull one (hw 926)
\input{macros/common}
\input{macros/print}
\title{Componentwise saturation-height bounds for relative class-number growth in the cyclotomic $\Z_2$- and $\Z_3$-towers\\[1ex]\large Blueprint: statements, dependency graph and full proofs}
\author{Hiroki Fukui}
\begin{document}
\maketitle
\noindent\textbf{Trust legend.} Every node title ends with its label: [F] Lean~4 kernel, toolchain v4.31.0-rc1, Mathlib d568c8c0, \texttt{\#print axioms} within [propext, Classical.choice, Quot.sound]; [C] certificate accepted by a read-only checker that replays it from the definitions: \texttt{scripts/\allowbreak family\_verify.sage} (sha256 0f38bb0d) for the $p=2$ family, \texttt{scripts/\allowbreak verify\_p3\_readonly.sage} for the $p=3$ covolumes (verifier step 04f); [L] published theorem used as an explicit hypothesis, verbatim location in the SOURCES block of \texttt{content.tex}; [M] proved in this document and in the paper, not machine-checked; [E] experiment, used in no theorem. Composite labels name the core and the inputs. Every F and M node carries a complete human-readable proof (single-source files \texttt{proofs/*.tex}); \texttt{\textbackslash leanok} marks kernel-checked statements only. Package: weber\_general\_n 1.0.0. This repository copy carries the round only; the manifest checksum and the local verifier run are recorded in \texttt{RELEASE\_STATUS.md}, while the commit SHA, the CI run and the tag live outside the source tree, in the release metadata and the CI attestation asset (a document cannot contain its own hash; GPT r14 sect 7). The two experiment nodes (\texttt{exp:class1}, \texttt{exp:svp}) are deliberately isolated: no theorem uses them.
\tableofcontents
\input{content}
\chapter*{References}
\input{\proofsdir/bibliography}
\end{document}
