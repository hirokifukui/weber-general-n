For $t=2,3,4$ at $n=7$ the equality $J_t=2^{t-1}R_n$ was established by exact rank computations of the $2$-adic filtration of $A_n$, constructed independently in Sage (\path{sage/r8_twoadic_depth_exact.sage}) and in Magma (\path{sage/r8_twoadic_rank_magma.m}), and replayed by a standalone Python rank checker that recomputes the rank of the saved bit matrices (\path{scripts/verify_twoadic_rank.py}, certificates in \path{certificates/twoadic_rank/}; log \path{sage/r9_scaling_no_go.log}); the Python step is a replay of stored certificates, not a third construction. Lemma~\ref{lem:oddtransfer} and Lemma~\ref{lem:depthfloor} hold for every $t\ge1$ (the latter relative to its literature inputs, Schinzel and MO16 eq.~(2.4)), so Theorem~\ref{thm:S0} applies at $t=2,3,4$ and the direct filtered-lattice routes at depths $4$, $8$ and $16$ are strictly weaker than Theorem~A.
