For $\chi$ of conductor exactly $q$ and $c\in\{2,6\}$, $\sum_{a\in(\Z/q)^\times}\chi(a)\log|1-\zeta^{ca}|=0$: $\zeta^{ca}$ depends only on $a\bmod q/2$, so the summand is constant on cosets of $U=\{1,1+q/2\}$, and $\sum_{u\in U}\chi(u)=1+\chi(1+q/2)=0$.
