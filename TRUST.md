# TRUST.md — what each label means and how to check it (weber_general_n, v1.0.1, 2026-08-29)

One raw cross-CAS log (sage/r10_bin4_xcas.log) retains a non-sensitive local machine name because its
byte-exact SHA-256 is part of the frozen evidence chain (certificates/constants/C7_cross_cas_r14.json);
the public-tree sanitizer exempts that one file from the host-name rule only.

Every claim of the paper carries exactly one label, or a composite of them; CORRESPONDENCE.csv
(generated from docs/CLAIMS_1.0.1.yaml) maps claim ids to Lean declarations, certificates, logs and
literature inputs. Earlier round-specific ledgers are preserved outside the submission-facing public tree. The blueprint (blueprint/src/content.tex) carries the same labels per node.

F  (Formal)      Proven in Lean 4 (toolchain v4.31.0-rc1, mathlib pin d568c8c0) with
                 `#print axioms` within [propext, Classical.choice, Quot.sound] and no sorryAx /
                 native_decide / ofReduceBool / trustCompiler. Check: step 09 of
                 scripts/verify_all_portable.sh (LEAN_WORKSPACE set), or `lake env lean <file>` in a
                 workspace with the pinned mathlib (environment/lean_version.txt). F covers the
                 DISCRETE reasoning only (group / quotient / index / coset steps, real inequalities,
                 the propositional chain from certificates to non-divisibility). It does NOT cover
                 Blichfeldt's theorem, heights, L-values, KY's saturation, or the checker's
                 implementation. \leanok in the blueprint is used only in this sense.
C  (Certificate) A finite machine-checkable certificate plus a READ-ONLY checker recomputing
                 everything from first principles in ball arithmetic. p = 3 covolumes (r19): certificates/p3/
                 D3_cert_r19.json, format v3 (every certified quantity as EXACT dyadic endpoints = the outward
                 hull of the producer balls and of the read-only recomputation, widened to twice its half-width;
                 generated by tools/gen_p3_cert_r19.py). THREE CHECKERS WITH SEPARATE ROLES, never called
                 as one checker: (1) tools/check_p3_cert.py = producer-record INTERNAL
                 CONSISTENCY + print strings (step 04e; recomputes the hull from the stored provenance); (2)
                 scripts/verify_p3_readonly.sage = SEMANTIC RECOMPUTATION from the definitions, trusting nothing
                 shipped (step 04f), with the COVERAGE gate that every shipped interval CONTAINS its recomputed
                 enclosure (120/120 = 8 load-bearing intervals x 15 rows; overlap is NOT accepted -- the r18 gate
                 accepted overlap, ERRATA_R19 E19-1) and twelve planted certificates rejected (ledger
                 docs/P3_NEGCTL_LEDGER_R19.md generated from sage/r19_trackB/p3_negctl_ledger_r19.json);
                 (3) tools/check_p3_containment.py = CERTIFICATE COVERAGE on a stored recomputation (step 04g).
                 Structured summaries (sage/r19_trackB/p3_readonly_summary_r19.json) carry the verifier version and
                 sha256; raw log hashes are forensic, never a mathematical gate. Family: scripts/family_verify.sage
                 (sha256 recorded in docs/CLAIMS_1.0.1.yaml) on certificates/family/KY1000/*.txt; soundness
                 spec theory/FAMILY_CERTIFICATE_SPEC_R10.md sect 4; n = 7 constants: rigorous digamma
                 interval certificate (certificates/constants/Cn_interval_r14.json, both endpoints to 160
                 digits) and the r6/r7 interval logs under certificates/blichfeldt/.
                 Nine planted negative controls (scripts/family_negctl.sh; ledger certificates/negctl/
                 negctl_ledger_r14.json) must all be REJECTED and the n = 2 positive control accepted; they
                 are a hard gate of the verifier. The GENERATOR is untrusted; only the verifier is trusted.
L  (Literature)  A published, proven theorem used as an explicit hypothesis. Three tags (introduced in r20): [used] = the statement the argument actually rests on, read verbatim from the primary
                 text on disk (paper/); [historical] = the source a used statement is attributed to by the text
                 we read, not itself read; [unread] = named for provenance only, not on disk. Used: KY Eq.(17),
                 Prop 4.1 + (23), Thm 2.3 (arXiv:2107.08587v3); MO 2016 Lemma 2.5(1),(2), Thm 2.7 (JTNB 28);
                 MO3 2020 Prop 6.6 (IJNT 16); MO 2013 Lemma 1.3 (Horie's saturation lemma, as stated and used
                 in MO13) with its proved general form Ho05a Prop. 1 (read r17); Washington GTM 83, cited at
                 CHAPTER level (Chapter 4: the log-cyclotomic-sum formula for L(1,chi) in absolute-value form and
                 |tau(chi)| = sqrt(f); Chapter 2: |disc Q(zeta_{p^r})| and the integral basis) -- the two
                 formulas are restated as Lemma D3 / App D and Lemma disc and their normalisations were checked
                 independently, so the result is cited at chapter level and no theorem number is used as a
                 logical input; Blichfeldt 1914 Thm II; Dirichlet.
                 Historical / unread: Ho05b (the relay MO13 cites for Lemma 1.3) and Ho02 (the original; paywall,
                 not read). "L-relative" = proven relative to the USED inputs and nothing else. No open conjecture
                 is ever an input (KY Conjecture 2.2, GRH, Bombieri-Lang: never).
M  (Manuscript)  Proved in the paper and, verbatim (single-source proofs/*.tex), in the Blueprint; NOT machine-
                 checked. Introduced in r12: Lemmas B, C (pullback bookkeeping), D, E, E', Prop F,
                 Prop D, Cor A', the depth-t floor, the old/new decomposition; r15: Theorem S0 (relative to its
                 hypothesis (i)) and Lemma oddtransfer (its number-field part; the group core is F). r16: Lemma B moved M -> F (WeberLemmaB); Theorem SH, Cor A-hat,
                 Cor order, Theorem P3, Cor P3n4 are M relative to their L inputs with F cores (WeberSH, WeberHatC, WeberP3) and, for
                 P3, a C covolume (certificates/p3/D3_cert_r16.json). r17: Theorem P3 is restated with (Rank) PROVED (Theorem rank3, F-core + L for Washington / Dirichlet) and the MO2016 relative-norm-one floor (Lemma mo25 [L], applicable by Lemma normone [F]); the certificate certificates/p3/D3_cert_r19.json (C; format v3, containment-gated read-only replay step 04f) enters only Cor P3n4 and the table; Theorem SH is stated in the carrier form proved in Lean and its module form is Cor SH-mod (M; F-core). r20: Theorem P3 is stated for ODD primes l != 3 only (the r19 wording admitted l = 2, ERRATA_R20 E20-1); the oddness is consumed at the M/F bridge -- Lemma normone (iv), where Nr(eps)^l = 1 with Nr(eps) real gives Nr(eps) = 1 through the F lemma WeberP3Rel.eq_one_of_odd_pow_eq_one (hypothesis Odd l), and in the hypothesis of Theorem SH; the Lean core WeberP3.theoremP3_core is the generic carrier (0 < l) and does NOT carry the odd-prime condition, which is therefore recorded here and in the claims (docs/CLAIMS_1.0.1.yaml THM_P3.conditions, checked against both statement texts by tools/check_statement_sync.py, step 13c). The shared proof of Theorem rank3 writes a == 4^{N/3} (mod q) as a congruence (E20-2; F: WeberP3Rel.four_pow_modEq). A reader checks M by reading;
                 the Blueprint's evidence-presence gate only checks that a proof body is PRESENT (check_graph.py --report flags short F/M proofs as warnings).
E  (Experiment)  Computation whose OUTPUT is reported but on which no theorem depends (class-1 scans,
                 escalation statistics, SVP minima, failure-class tables, C_n for n >= 8 beyond the
                 rigorous enclosure). E-material can be wrong without affecting any F/C/L claim.

Composite labels: "L-relative, F-core" (Theorem A: the discrete implication is F, the inputs are L);
"C+L, F-core" (certificate soundness / family theorem: certificate checked, inputs L, propositional
core F — the Lean layer does NOT verify the checker implementation); "C + F" (Cor 7: the
enclosure is C, the integer comparison is F).

Ledger vocabulary: [P] proof written and checked (F or L-relative); [MC] CAS-confirmed numerics;
[H] unproven idea; [OPEN] not decided. Word freeze: no "first", "new principle", "effective
complement", "tower-wide" until the novelty audit is complete.

Replay. scripts/verify_all_portable.sh is the single command (steps listed in its header; r12 added
environment asserts, the KY1000 target-list verifier with negative controls, per-prime ledger comparison,
the C_n JSON certificate check, the Blueprint evidence-presence gate and PDF build, and the map check; r13 adds
the threshold-synchronisation check (tools/check_floor_sync.py, E13-1), the NORMALIZED per-prime comparison
(tools/ledger_summary.py: verdict / components / witness sha / line counts / RHO and T counts / max T upper /
min rho upper — the raw verifier-log sha256 is carried as a forensic field and is no longer a gate, because raw
logs need not be byte-identical across platforms), nine negative controls plus the n = 2 positive control, a
fifth Lean file, and the Blueprint human-proof gate).
Executed end-to-end on the author's node <LOCAL_HOST> with Sage 10.8 on 2026-08-25 (r11; r12 and r13 runs recorded in RELEASE_STATUS.md):
verify_out/VERIFY_ALL_PORTABLE.log. Dockerfile (sagemath/sagemath pinned by digest sha256:e2e4747b..., + texlive) and
.github/workflows/verify.yml (sage job + lean job with `lake exe cache get`; actions and elan pinned by
SHA; Lean workspace outside the checkout) are written but have NOT been executed anywhere yet — the
author's machines have no docker. The first GitHub Actions
run after push is the clean-environment record; until it exists, no clean-container claim is made.

Known limits of the self-audit (recorded, not excused). r12 was sealed with every medium passing its own gate
while the prose and the Blueprint definition of the verifier stated the certificate T-threshold as the uniform
33 * 2^n, which is wrong at n = 2 (the verifier and the literature use 17 * 2^n there; E13-1, found by the
external reviewer). The gates checked code, Lean and prose separately; none checked that they state the same
constant. r13 adds that check (tools/check_floor_sync.py, verifier step 00b) and the rule that the
correspondence between a code constant, its Lean definition and its prose statement is itself a gated claim.
The same class of error — a self-reported gate name promising more than the check performs — is the reason the
r12 "proof-completeness gate" is renamed evidence-presence gate in r13.

Provenance of every number: the sage/ log named in CORRESPONDENCE.csv; hashes of every shipped
file in MANIFEST_SHA256.txt (HANDOFF.md and verify_out/ are excluded by design, see tools/gen_manifest.py).
