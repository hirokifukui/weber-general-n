# Cn_digamma_r12.json - SUPERSEDED at r14 (E14-1)

Moved here from certificates/constants/ on 2026-08-26 (R14, Dr. Fukui ruling). It was generated by the r12 tools/gen_cn_certs.py by
scraping sage/r11_propD_audit.log; its C_lo / C_hi strings are RealField(80)/(120) roundings of the 500-bit ball endpoints, printed at
25-38 digits, so both ends are the same string and the trailing digits (e.g. "...834061" at n = 7) are binary-rounding artifacts, not the
decimal expansion. It cannot reconstruct the enclosure. Replaced by certificates/constants/Cn_interval_r14.json (format 2; both endpoints
to 160 digits, rounded outward; produced by sage/r14_cn_interval.sage). Kept only so that r12/r13 packages remain auditable.
The parts (a)-(d) of sage/r11_propD_audit.log (Gram eigenvector check, prod |1 - chi(3)| = 2, Prop D cross-CAS) are still live evidence
for lem:D0 / lem:prod13 / prop:D; only its part (e) (C_7 printing and the "inside ball" tolerance test) is superseded (E14-1, E14-3).
